apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: google-connect-credentials
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: google-connect-credentials
    creationPolicy: Owner
  data:
    - secretKey: credentials.json
      remoteRef:
        key: onePasswordJsonConfig